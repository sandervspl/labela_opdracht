// dependencies
import React, {} from 'react';

// style
import './Loading.styl';

const Loading = () => (
  <p className="loading">Loading...</p>
);

Loading.propTypes = {};

export default Loading;
